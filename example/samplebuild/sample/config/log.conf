{
	"InitAppenders":["console"],
	"LogGroups" :{
		"globalGroup" :{
			"Appender":["console"],
			"none":false,
			"ConsolePattern":"${yyyy}-${MM}-${dd} ${hh}:${mm}:${ss}${SSSSSS} [${TARGET}] ([${LOG_GROUP}][${LOG_TAG}][L${FILE_LINE} ${FUNC_NAME}])\n${MSG}"
		}
	}
}